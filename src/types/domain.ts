// Domain types are inferred from Zod schemas — schemas are the source of truth.
export type {
  BandOption,
  Company,
  Organization,
  Person,
  Position,
  AffiliationType,
  Affiliation,
} from '../domain/schemas'
