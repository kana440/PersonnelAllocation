// 降格理由 — AllocationList.demotionReason / Operation.demotionReason
// Pure list — organization-specific values, no additional attributes
import type { CodeEntry } from './types'

export type DemotionReasonEntry = CodeEntry
