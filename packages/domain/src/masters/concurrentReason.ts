// 兼務理由 — AllocationList.concurrentReason / Affiliation.concurrentReason
// Pure list — organization-specific values, no additional attributes
import type { CodeEntry } from './types'

export type ConcurrentReasonEntry = CodeEntry
